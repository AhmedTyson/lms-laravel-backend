# Changelog — one file per day

Append today's changes to `YYYY-MM-DD.md` (UTC date). One bullet per commit or coherent change set. Link rule/SCOPE ids when behavior changes.

Format per entry: `hash` + what + why (one line each).
