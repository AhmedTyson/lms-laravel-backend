# Assignments Routes — `/api` (planned, Phase 8)

No endpoints yet. `routes/api.php` intentionally empty.

Planned (spec 6.4, RULE-015/016): `GET/POST /courses/{id}/assignments`, `PATCH/DELETE /assignments/{id}`, `POST /assignments/{id}/submissions` (attempt-unique, resubmit guard), `GET /assignments/{id}/submissions`.
