# Enrollment Routes — `/api` (planned, Phase 7)

No endpoints yet. `routes/api.php` intentionally empty.

Planned: `POST /enrollments` (enroll, unique student+course), `DELETE /enrollments/{id}` (withdraw), `GET /me/enrollments`.
