# Progress Routes — `/api` (planned, Phase 10)

No endpoints yet. `routes/api.php` intentionally empty.

Planned (RULE-018 revised, RULE-019): `GET /enrollments/{id}/progress`, `POST /completions` (override path with `overridden_by`), `GET /me/progress`.
