# Notifications Routes — `/api` (planned, Phase 11)

No endpoints yet. `routes/api.php` intentionally empty.

Planned (spec 6.7): `GET /notifications`, `POST /notifications/{id}/read`, `DELETE /notifications/{id}`.
