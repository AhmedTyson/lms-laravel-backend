# Courses Routes — `/api` (planned, Phase 6)

No endpoints yet. `routes/api.php` intentionally empty.

Planned (spec 6.2, 6.3): `GET/POST /courses`, `GET/PATCH/DELETE /courses/{id}`, `POST /courses/{id}/publish`, `POST /courses/{id}/archive`, `GET/POST /courses/{id}/lessons`, `PATCH/DELETE /lessons/{id}`, lesson reorder.
