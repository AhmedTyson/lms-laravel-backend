# Quizzes Routes — `/api` (planned, Phase 9)

No endpoints yet. `routes/api.php` intentionally empty.

Planned (spec 6.5, SCOPE-005, RULE-017): `GET/POST /courses/{id}/quizzes`, `PATCH/DELETE /quizzes/{id}`, `GET/POST /quizzes/{id}/questions`, `POST /quizzes/{id}/attempts`, `POST /attempts/{id}/answers`, `POST /attempts/{id}/submit`.
