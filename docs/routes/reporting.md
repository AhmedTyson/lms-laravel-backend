# Reporting Routes — `/api` (planned, Phase 12)

No endpoints yet. `routes/api.php` intentionally empty.

Planned (spec 6.8, ADR-008): `GET /reports/enrollments`, `GET /reports/completions`, `GET /reports/courses/{id}`. Explicit team context on every query.
